import { SaltoDeLineaPipe } from './salto-de-linea.pipe';

describe('SaltoDeLineaPipe', () => {
  it('create an instance', () => {
    const pipe = new SaltoDeLineaPipe();
    expect(pipe).toBeTruthy();
  });
});
