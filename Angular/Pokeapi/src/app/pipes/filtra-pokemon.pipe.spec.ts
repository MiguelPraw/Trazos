import { FiltraPokemonPipe } from './filtra-pokemon.pipe';

describe('FiltraPokemonPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltraPokemonPipe();
    expect(pipe).toBeTruthy();
  });
});
